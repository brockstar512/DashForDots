using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    public Color32 green = new Color32(56,210,121,255);
    public Color32 red = new Color32(254, 61, 106, 255);
    public Color32 gray = new Color32(101,138,167,255);
    public Color32 yellow = new Color32(255,187,50,255);
    public Color32 purple = new Color32(155,107,255,255);
    float bigText;
    float smallText;
    float buttonText;









    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
